#############################################
#  Pasta pública (public/)
#############################################

# 1) Arquivo padrão explicitamente definido
DirectoryIndex index.php

# 2) Impede listagem
Options -Indexes

# 3) Bloqueia qualquer arquivo/pasta oculta
<FilesMatch "^\.">
    Require all denied
</FilesMatch>

# 4) (Opcional) Se houver outros .php aqui, bloqueie‑os
#    Caso só exista o index.php, você pode omitir esta seção.
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>
<FilesMatch "^index\.php$">
    Require all granted
</FilesMatch>

# 5) Cabeçalhos de segurança adicionais
<IfModule mod_headers.c>
    Header always append X-Frame-Options "SAMEORIGIN"
    Header set    X-Content-Type-Options "nosniff"
    Header set    X-XSS-Protection "1; mode=block"
</IfModule>
